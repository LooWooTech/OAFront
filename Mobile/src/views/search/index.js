import React, { Component } from 'react';
import { Container, Header, Left, Body, Right, Content, List, ListItem, Input, Button, Icon } from 'native-base'
class Search extends Component {
    render() {
        return (
            <Container>

            </Container>
        );
    }
}

export default Search;